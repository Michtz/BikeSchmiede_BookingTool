import { FC } from 'react';
import ArticleContainer from '@/components/containers/ArticleContainer';

const TestSchmolkeContainer: FC = () => (
  <ArticleContainer article={'schmolke'} />
);

export default TestSchmolkeContainer;
